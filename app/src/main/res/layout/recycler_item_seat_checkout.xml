<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable name="position" type="Integer"/>
        <variable name="seat" type="com.rsl.youresto.data.checkout.model.SeatPriceModel" />
        <variable name="seatsAdapter" type="com.rsl.youresto.ui.main_screen.checkout.seats_checkout.SeatsCheckoutAdapter" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:padding="1dp"
            android:onClick="@{v -> seatsAdapter.onSeatClicked(seat, position)}"
            android:background="@drawable/background_white_border_black">

        <TextView
                android:id="@+id/text_view_seat_no"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:padding="4dp"
                android:text='@{`S` + seat.MSeatNo + `: ` + String.format("%.2f",seat.MSeatTotal)}'
                android:textColor="@{seat.MPaid ? @color/colorWhite : seat.selected ? @color/colorWhite : @color/colorBlack}"
                android:background="@{seat.MPaid ? @color/colorGreen : seat.selected ? @color/colorPrimary : @color/colorWhite}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:textAppearance="@style/Base.TextAppearance.AppCompat.Medium"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="S10: 100"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>